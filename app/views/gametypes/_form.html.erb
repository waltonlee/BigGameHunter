<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@gametype) do |f| %>
      <% if @gametype.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@gametype.errors.count, "error") %> prohibited this gametype from being saved:</h2>

          <ul>
          <% @gametype.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :name, class: "control-label" %><br>
        <%= f.text_field :name, class: "form-control" %>
      </div>

    <div class="form-group">
        <%= f.label :image, class: "control-label" %><br>
        <%= f.text_field :image, class: "form-control" %>
      </div>

      <div class="actions">
        <%= f.submit "Submit", :class => "btn btn-default btn-primary" %>
      </div>
    <% end %>
  </div>
</div>